name: Fetch and Save Apartment Data

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install requests pandas

      - name: Run Python script
        env:
          API_KEY: ${{ secrets.SERVICE_KEY }}
        run: |
          python fetch_data.py
          ls -lh apt_trade.db

      - name: Create SQL dump file
        run: |
          sqlite3 apt_trade.db .dump > apt_trade.sql
          ls -lh apt_trade.sql

      - name: Replace double quotes with backticks # 이 부분을 추가하세요
        run: |
          sed -i 's/"/`/g' apt_trade.sql
          sed -i 's/ ON CONFLICT REPLACE//' apt_trade.sql
          
      - name: Commit and push DB & SQL files
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "feat: Update apartment trade data DB and SQL files"
          file_pattern: 'apt_trade.db apt_trade.sql'
